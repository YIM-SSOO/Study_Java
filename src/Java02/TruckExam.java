package Java02;

public class TruckExam {
    public static void main(String[] args) {

        Truck truck = new Truck();

        //Car4 c = new Car4();

        // 생성자가 하는 일은 객체를 초기화하는 일
        // 생성자가 호출될 때 자동으로 부모의 생성자가 호출되면서 부모객체를 초기화하게 된다.
        // 부모의 생성자를 호출할 때는 부모 객체를 나타내는 super라는 키워드를 사용하게된다.
        // 부모생성자는 super에 괄호 열고 닫고를 붙이면 부모의 생성자가 호출되게 된다.


    }
}
